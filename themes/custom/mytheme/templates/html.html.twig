<!DOCTYPE html>
<html{{ html_attributes }}>
  <head>
    {{ head }}
    <head-placeholder token="{{ placeholder_token }}">
    <title>hi</title>
    {{ styles }}
    {{ scripts }}
  </head>
 <body class="{{ attributes.addClass(theme_color_class) }}">

    {{ page_top }}
    {{ page }}
    {{ page_bottom }}
  </body>
</html>
